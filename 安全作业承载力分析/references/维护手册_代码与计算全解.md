# 安全作业承载力分析 · 维护手册

> 技能「安全作业承载力分析」维护入口：计算规则、函数职责、配置、报告链路。
> 公式口径权威来源《承载力量化分析方法.md》。改动后双份同步（§7）。

---

## 0. 快速导航

| 需求 | 位置 |
|---|---|
| 计算规则/公式 | §2 |
| 配置文件改参数 | §3 |
| 各文件/函数职责 | §1、§1.1（全函数目录） |
| 报告生成链路 | §5 |
| 报错排查 | §6 |
| 修改纪律 + 自检 | §7 |

---

## 1. 目录结构（一文件一句话）

```
安全作业承载力分析/
├─ config/capacity_config.json      # ★唯一配置：公式系数 + 输入解析
├─ references/                      # 参考文档（只读）：公式口径/作业计划字段/报告模板字段/部署/本手册
├─ templates/                       # 6 份官方公文模板（填充只读不覆盖；月报图题 v2.17 已按阅读序维护）
└─ scripts/
   ├─ calcCapacity.py               # ★计算引擎 CLI 入口（顶注释有函数索引）
   ├─ genReportParams.py            # ★result→fillReport 填充参数（概况/明细/评述/管理段/矩阵表）
   ├─ genLetterParams.py            # ★函件成套参数：calc result→逐封 fillReport 参数（警示函/提示函）
   ├─ fillReport.py                 # ★模板填充（Word COM）：参数→正文草稿
   ├─ pngCharts.py                  # ★PNG：render 出图 + apply 插入（原两文件合并）
   ├─ nativeCharts.py               # ★薄入口 → capacity.office_chart.native_main
   ├─ capacity/office_chart.py      # ★DrawingML 注入 + 版面策略 + native 编排
   ├─ runReports.py                 # ★一键编排：计算→参数→填充→图表（--chart native|png）
   └─ capacity/
      ├─ constants.py               # 常量/θ/β/错误码/统一异常
      ├─ models.py                  # 领域模型（Person/Plan/Absence/WorkDataset）
      ├─ config.py                  # 读配置 + 公式参数写回 constants
      ├─ parsing.py                 # 解析校验（JSON/接口/表格行）
      ├─ excelLoader.py             # Excel 双引擎（.xls→xlrd，.xlsx→openpyxl）
      ├─ coefficients.py            # β/θ 取值、四级预警分级
      ├─ org.py                     # 组织架构归一化→17 单位列
      ├─ calculator.py              # ★计算核心（班组/负责人/工区/周期/矩阵）
      └─ output.py                  # 姓名脱敏/原子写盘/demo
```

数据流：`输入(Excel/JSON/接口) → parsing+excelLoader → WorkDataset → CapacityService → 输出JSON → 报告脚本`。

**常用函数速查**（均在 `calculator.py`，入口在 `calcCapacity.py` 顶注释）：

| 想看 | 函数 |
|---|---|
| 班组承载力 | `calc_team_capacity`（内拆 `calc_leader_capacity` / `calc_member_capacity`） |
| 工区承载力 | `calc_area_capacity` |
| 周期聚合 | `calc_period`（`calc_period_buckets` 分周/月/年） |
| 日期×单位矩阵 | `build_matrix_day` |
| 预警分级 | `coefficients.classify_alert` |
| 组织架构归列 | `org.UnitNormalizer.unit_of`（sheet 上下文消歧 → 关键词兜底，见 §2.6.1） |
| 函件触发筛选 | `genLetterParams._build_letters`（满载/重载/长期作业，见 §5.1） |
| θ/β 取值 | `coefficients.risk_theta` / `build_person_beta` |
| 管理承载力 | `build_management`（内拆 `managers_by_unit` / `management_demand_by_day`） |
| 原生图位定位 | `renderNativeCharts.locate_slots`（按图题识别 13 图位=段序+图号+图题）；`manage_period_mean`/`plan_type_count` 取数据（复用 renderReportCharts） |
| 原生 Chart 落地 | `renderNativeCharts.add_native_chart`（Select 图位段 → 无参 AddChart → ChartData.Workbook 写数据 → SetSourceData） |

---

## 1.1 全函数目录（读这一节即懂每个函数）

> 一行一函数、签名从简、代码为准；`_` 前缀为模块内私有。计算内核在 `scripts/capacity/`，可执行脚本在 `scripts/`。数据流：`输入 → parsing/excelLoader → WorkDataset → CapacityService → 结果JSON → 报告脚本`。

### capacity/constants.py（常量与异常）
- `is_workday(d)`：该日是否计入**周期累加分母**（剔周末[EXCLUDE_WEEKENDS]与 STATUTORY_HOLIDAYS）；日维度不调用。
- 常量：`STD_HOURS_PER_DAY=8`、`MIN/MAX_WORK_HOURS[1,8]`、`DEFAULT_BETA=1.0`、`FORCE_BETA=True`、`LEADER_LOAD_CAP=999`、`DENOM_EPS`、`RISK_COEF`(θ表)、`ALERT_RULES`(四级)、`REPORT_UNITS`(17单位)、各 `*_FIELD_ALIASES`(列别名)。
- 异常：`CapacityError`(基类，带 code/context/to_payload) 及 `UsageError/InputDataError/FileAccessError/FileContentError/OutputWriteError`；错误码 `ERR_*`、退出码 `EXIT_*`、`_ERROR_EXIT` 映射。

### capacity/models.py（不可变领域模型 @dataclass frozen）
- `PersonRecord`：姓名/班组/beta/is_leader/county/work_area/role。
- `PlanRecord`：名称/班组/leader/`leader_count`(负责人需求数量)/members/hours/risk/plan_type/start/end/company/work_area。
- `AbsenceRecord`：姓名/起/止（不在岗）。
- `WorkDataset`：persons/plans/absences 三元组，上层唯一数据源。

### capacity/config.py（配置加载与写回）
- `load_config(path)`：读 JSON 配置；缺省自动找，非法抛异常。
- `apply_formula_config(cfg)`：把「公式与计算参数」写回 constants 模块级系数（θ/预警/非工作日/管理系数）。
- `default_config_path()`：向上查 `config/capacity_config.json`。
- `_cfg_get(node,key,default)`：多级安全取值。
- 数据：`DEFAULT_INPUT_CONFIG`(内置输入解析缺省)、`_PLAN_CANON`(逻辑字段→模型字段)。

### capacity/parsing.py（解析校验：原始→模型）
- `parse_date(v)` / `coerce_date(v,mode)`：日期解析（含 Excel 序列号 1899-12-30 基准）。
- `to_float` / `clamp_hours` / `_parse_bool` / `_pick` / `_require_date`：字段转换与别名取值。
- `parse_person` / `parse_plan` / `parse_absence`：单条校验（含 leader_count 数量解析、时长 clamp[1,8]）。
- `build_dataset(raw)`：{persons,plans,absence}→WorkDataset。
- `read_dataset_file(path)`：读 JSON 文件→WorkDataset。
- `_rows_to_plans(rows,cfg)` / `_rows_from_interface` / `read_interface_dataset`：表格行/接口 JSON→计划。

### capacity/excelLoader.py（Excel 双引擎）
- `_open_workbook(path)`：按扩展名选 xlrd(.xls/.xlsm) / openpyxl(.xlsx)。
- `_sheet_nrows/_sheet_ncols/_sheet_cell/_find_workbook_sheet/_xls_rowdicts`：跨引擎取值。
- `_build_xls_persons(wb,cfg)`：解析人员表（姓名/班组/角色必填，β/工区/职务可选）。
- `_parse_workbook`：主表+人员表→WorkDataset。
- `read_sheet_rows(path,sheet,...)`：读任意 sheet 原始行（组织架构/展示列复用）。
- `read_xls_persons(path,cfg)`：只读人员表原始记录。
- `read_xls_dataset(path,cfg)`：Excel 作业计划→WorkDataset（入口）。

### capacity/coefficients.py（系数与预警）
- `risk_theta(risk_level,plan_type=None)`：作业风险 θ（装表按五级；无风险 0；未知回退四级 1.0）。
- `build_person_beta(persons)`：班组→姓名→β 索引 + 班组→负责人β。
- `classify_alert(pct)`：四级预警分级（降序命中）。

### capacity/roster.py（班组真实分母 / 负责人真名）
- `clean_team_token` / `split_team_tokens`：班组名清洗（去 -N 抽调）与多班组拆分。
- `build_roster_index(persons)`：班组→{beta_by_name,leaders,areas,counties}。
- `resolve_team(roster,team)`：计划班组串→{teams,staff,leaders,names}；任一未命中返回 None。
- `roster_staff(roster,team,absent)`：当日可用人数（Σβ−不在岗）。
- `filter_roster_matchable(dataset,roster)`：剔除班组匹配不到的计划。

### capacity/org.py（组织架构归 17 单位列）
- `UnitNormalizer`：归一化器——
  - `load_sheet(rows)`：载入「组织架构」sheet 目录（同名多子树并存）。
  - `unit_of(values)`：公司/工区/班组→单位列（sheet 上下文消歧 → 关键词兜底）。
  - `short_name(values)`：取公司/工区/班组简称。
  - `_resolve_context/_find_row/_pick_candidate/_unit_from_row/_match/_slice/_short_company`：消歧与匹配内部件。
- `load_org_sheet_rows(source,cfg)`：配置启用时读源表「组织架构」sheet。
- `filter_matchable(dataset,normalizer)`：剔除归「其他」的计划。

### capacity/calculator.py（★计算核心）
- `_days_between` / `_workdays_between`：自然日 / 工作日展开。
- `period_unit_caps_from_daily` / `period_manage_caps_from_days`：图表与评述的周期口径（Σ自然日 / 工作日天数；管理=Σ实需÷(在册×工作日)）。
- `_day_absent_names` / `_on_day_plans` / `_team_denom`：当日不在岗 / 生效计划 / 班组分母(8×可用人数)。
- `calc_team_staff`：班组可用人数（真实在册 − 当日不在岗）。
- `calc_leader_capacity`：负责人承载力（需求工时 ÷ 负责人可用人天，≤999）。
- `calc_member_capacity`：班组成员承载力（成员工时 ÷ 分母）。
- `calc_team_capacity`：班组综合 `max(负责人,成员)`+班组类型扩展，含预警与单位归列。
- `calc_area_capacity`：工区加权承载力（权重=自身占比）。
- `calc_day`：单日逐班组聚合（第二遍做每人 8h 截断）。
- `calc_period`：周/月/年聚合（分子=全部自然日计划工时，分母按工作日日历）。
- `calc_period_buckets`：按周(ISO)/月/年分桶。
- `build_matrix_day`：单日「日期×单位」矩阵行。
- `_manage_occupancy`：单现场管理占用（同进同出 2 / 一般 1）。
- `managers_by_unit`：各单位在家管理人数（v2.5 起仅展示）。
- `staff_by_unit`：各单位在册 Σβ（管理承载力分母）。
- `management_demand_by_day`：逐日各单位实需人天（自然日）。
- `_mgmt_unit_summary` / `_mgmt_range_summary`：单位 / 区间管理承载力汇总（分子=全部自然日实需，分母天数按工作日）。
- `build_management`：管理承载力全量输出（managers/overall/periods/days）。
- `CapacityService`：薄调度——`__init__`(β索引/归一化器/剔除不匹配)、`calc_day`/`calc_period`、`summarize(period)`（输出 persons_beta/days/periods/matrix/management）。

### capacity/output.py（输出 / 演示）
- `mask_name` / `mask_result_names`：姓名脱敏（`MASK_NAMES=False`，现网**真名不脱敏**）。
- `atomic_write_text(path,content)`：临时文件 + `os.replace` 原子写。
- `read_demo()`：内置演示数据。

### scripts/calcCapacity.py（计算 CLI 入口）
- `build_main_parser` / `_setup_logging` / `_load_dataset` / `main`：参数路由(demo/xls/api/json) → 配置 → 计算 → 输出 JSON。

### scripts/genReportParams.py（结果→报告填充参数）
- 工具：`_fmt_d/_overlaps/_risk_num/_iso_week/_month_bounds/_month_weeks/_fmt_plan_time`。
- 取数：`_load_source/_load_result/_unit_cap_by_day/_mgmt_aggregate`。
- 文案：`_manage_sentence/_build_job_block/_week_over_analysis/_weekly_job_paragraph/_works_text`。
- 明细/表：`_build_summary/_select_detail_plans/_build_detail_rows/_unit_profile/_matrix_table/_week_mean_caps/_manage_sentence_per_week`。
- 组装：`build_daily_params/build_weekly_params/build_monthly_params`（`_BUILDERS` 分发）。
- 建议：`_resolve_advice/load_advice_payload`（`--advice` 注入文末建议章，缺则 `DEFAULT_ADVICE` 兜底）。
- `main`：装配 `replace/paragraph_replace/tables` 并原子写盘。

### scripts/genLetterParams.py（结果→函件参数）
- `_load_jobs/_load_roster/_attach_roster`：作业视图 + 人员档案 + 负责人真名匹配。
- `_period_meta/_unit_max_caps/_pick_full_or_heavy/_long_shift_teams`：周期与触发筛选（满载>90 / 重载(75,90] / 长期作业连续≥阈值）。
- `_letter_rows/_common_replace/_long_letter_replace/_finalize_replace/_safe_filename/_fmt_work_date`：逐封 replace + 10 列清单、文件名全角化。
- `_build_letters` / `main`：逐封组装并写盘（无命中空数组，非错误）。

### scripts/fillReport.py（模板填充引擎，Word COM）
- `begin_shared_word/end_shared_word/current_word_app/_launch_word_app`：流水线共用隔离 Word/WPS；直接 CLI 不调用则各脚本自行起实例。
- `WordSession`——
  - `render(template,params,out,out_as_doc)`：只读打开模板 → 替换/填表 → `trim_month_weeks` 删多余周节 → 另存。
  - `_apply_replace`：全文子串替换（先长后短、段内重写防吞段）。
  - `_apply_para_replace`：anchor+occurrence 段落级替换（`next_paragraph` 整段覆盖）。
  - `_trim_extra_month_weeks`：月报实际周数 <5 时删多余周节（含 5 月示例）。
  - `_fill_table/_fill_table_data/_fill_matrix_chunked`：附表定位（anchor/表头评分）与填充、矩阵超 8 列纵切分块。
  - `_compact_matrix_table/_rebalance_matrix_widths/_append_matrix_table`：分块行高 / 列宽 / 续表。
  - `_cell_text/_table_headers/_match_score/close`：辅助与收尾（共用实例时 close 不 Quit）。
- `_replace_next_paragraph/_demo_params/main`：段后整段替换 / 演示参数 / CLI。

### scripts/pngCharts.py（PNG 渲染+插入，v2.26 合并）
- `render_main`：原 renderReportCharts（竖柱 PNG）。
- `apply_main`：原 applyCharts（插图、fit_tables、repair_layout）。
- CLI：`pngCharts.py render|apply ...`。

### scripts/nativeCharts.py + capacity/office_chart.py（native + 版面，v2.26）
- `nativeCharts.py`：薄入口 → `office_chart.native_main`。
- `office_chart`：DrawingML 注入、`apply_month_fixed_pages`、`reflow`→`polish_month_docx`；**版面策略唯一源**（原 report_layout）。

### scripts/runReports.py（★一键编排 CLI）
- `_pipeline` / `_native_pipeline`：png 走 `pngCharts`；native 走 `nativeCharts`。
- **v2.14 性能**：同进程 main；`--all` 只 calc 一次；共用 Word + v2.25 recycle。

---

## 2. 计算规则速查（改哪看哪）

### 2.1 系数与工时

| 项 | 默认 | 修改入口（config） |
|---|---|---|
| 标准日工时 | 8h | 公式节.标准日工时 |
| 时长 T 边界 | [1h,8h]（不足1h按1h，超8h按8h） | 公式节.工时边界 |
| 人员效能 β | 统一 1.0（FORCE_BETA=true） | 公式节.强制统一β=false 后接真实档案 |
| 班组可用人数分母 | 人员档案真实在册（Σβ）；多班组求和；**无默认满编回退，匹配不到整条剔除** | 见 `roster.py`（人员档案为必要数据源） |
| 负责人钳制 | ≤999 | 公式节.负责人承载力上限 |

### 2.2 作业风险系数 θ（`risk_theta`）

| 等级 | 二级 | 三级 | 四级 | 五级 | 装表接电 |
|---|---|---|---|---|---|
| θ | 1.5 | 1.2 | 1.0 | 0.8 | 0.2 |

- 作业类型含「装表」→ 一律按五级 θ=0.8；无风险等级 → θ=0（不计入）；未知 → 回退四级 1.0。

### 2.3 日承载力（分子/分母，标准 8h）

```
负责人工时 += T·θ·β_leader×leader_count；成员工时 += T·θ·1.0×members
巡视工时（作业∈巡视）　+= T·members；操作工时（∈倒闸/操作）+= T·members
可用人数 = 班组真实在册Σβ − 当日不在岗Σβ（多班组串求和；≤0 按 0，**无默认满编回退**）
```
- **负责人承载力** = `min(负责人工时 / (8×该班组工作负责人数), 999)`（工作负责人数=人员档案中该班组角色含「工作负责人」的真名个数；分子按 `计划所需负责人数量 leader_count` 计需求）
- **班组分母**：取自人员档案真实在册（`roster.py` 按 班组 清洗/拆分匹配；**任一班组匹配不到 → 该计划整条剔除，不计算**）
- **成员承载力** = `成员工时 / (8×可用人数)`（成员工时=0 → 0）
- **班组承载力**：检修施工 `max(负责人,成员)`；运检合一 `+巡视`；变电运维 `操作+巡视`
- **工区承载力** = `Σ(v_i·(v_i/Σv))`（权重=自身占比；全 0 不输出 area）
- **每人每日“实际工时”≤8h 截断**（v1.5，口径：一个人工作时间超 8h 按 8h 计，ΣT>8 时按 `8/ΣT` 比例压缩，`calc_day` 聚合后第二遍处理）：
  - 工作负责人**按人分组**：单日跨多个作业 ΣT>8h 时，各作业加权需求按 `8/ΣT` 比例压缩后再求和（`负责人分组` 字典）；
  - 班组成员：按“当日班组工作日小时合计 ΣT”为每人实际工时（班组人员同做本班组全部作业），成员工时/巡视/操作占用按 `8/ΣT` 比例压缩（= 计划口径「ΣT×人数 > 8×人数」消去人数后即 ΣT>8）。

### 2.3.1 工作日日历（v2.3：日维度按自然日、周期累加才剔除）

统一抽象为 `C.is_workday(d)`（constants.py）。**现行口径（代码为准）**：
- **默认剔除**：`constants.EXCLUDE_WEEKENDS=True`；config「非工作日.是否剔除周末」现值 **true**；config「非工作日.法定节假日」现列 **2026 全年**放假清单（元旦/春节/清明/劳动/端午/中秋/国庆），由 `config.apply_formula_config` 写入 `STATUTORY_HOLIDAYS`。
- `is_workday(d)` = 非周六周日（EXCLUDE_WEEKENDS 时）且 `d ∉ STATUTORY_HOLIDAYS`。需完全按计划时间算时，把「是否剔除周末」置 false 且清空节假日清单。

**v2.3 关键：`is_workday` 只作用于周期累加分母，日维度一律按自然日展开**：
- 日维度（`CapacityService.summarize` 的 `day_set`→`days`/`matrix`、`management_demand_by_day` 的 `management.days`、周报矩阵 `_matrix_table`、日分析 `_week_over_analysis`）走 `_days_between`，含周六周日/法定节假日，有计划的天照常算、周末承载力与日常一致；
- 周期累加（`calc_period`、`_mgmt_range_summary`/`genReportParams._mgmt_aggregate`）：**分母**走 `_workdays_between`/`is_workday` 剔除周末与法定节假日；**分子**按区间全部自然日累加计划工时/实需人天（v2.15：此前 `continue` 把分子也跳掉了，已纠正）。

计划本身的排期日期（如明细表“工作日期”、函件作业清单附表）为**计划事实**，按原日期照实展示、永不因日历改写。

### 2.4 周期聚合（周/月/年）

分子按区间**全部自然日**叠加（周六日/法定节假日有计划的工时照计）；**分母按工作日日历剔除周末+法定节假日**（`is_workday`，见 §2.3.1）；`F_班组 = max(负责人,成员)`；分桶键 周=ISO周 / 月=`YYYY年M月` / 年=`YYYY年`。**周期聚合不做每人 8h 截断**（现网简化口径；日/周/月报告正文的 `periods` 块同此，与日明细 §2.3 的 8h 压缩有差异，属既定口径）。周/月图表与评述（v2.16）同此分子分母：单位柱高=Σ自然日日承载力%÷工作日天数；管理柱高=Σ实需÷(在册×工作日天数)；不是有数据日的算术平均。区间无工作日时分母改用自然日（周六日报不画成 0）。

### 2.5 预警分级（`classify_alert`，严格>降序命中）

| 轻载(蓝) | 适度(黄) | 重载(橙) | 满载(红) |
|---|---|---|---|
| ≤50% | 50~75% | 75~90% | >90%（**可超 100%**，v1.5 删「停工(超满载)」档，>100% 落满载红） |

边界注意：恰为 50/75/90 判**上一档**（严格大于）。承载力可超 100% 正常显示（例：某班组成员工时 145.7% → 预警「满载」）。

### 2.6 日期×单位矩阵（组织架构启用时输出）

单位承载力 = 该单位各班组成员能力**加权均值**（权重=自身占比），未命中归「其他」。

### 2.6.1 组织架构归列（sheet 数据驱动 + 关键词兜底）

归列把计划的 公司/工区/班组 三字段收敛到报告矩阵表 17 单位列，解析优先级：

1. **源表「组织架构」sheet 上下文消歧**（配置 `组织架构映射.优先读取源表组织架构=true` 且源为 Excel 时启用）：按班组名/工区名在 sheet（市公司/县公司/县公司简称/单位（工区）/工区简称/班组/班组简称）中收候选行，用 计划公司→县公司子树、工区→单位（工区）/工区简称 逐级打分，唯一且得分>0 才采纳——即便同名行在 sheet 唯一出现也必须与计划公司子树重叠，防「配电工程四队」等外部承接队在直属计划下被错归到县公司。命中行的「县公司简称∈单位清单」直接归该县公司；否则对行内 单位（工区）/工区简称/班组/班组简称 名走关键词归属规则。
2. **关键词归属规则**（`班组归属规则` 顺序匹配任一「包含」词）——sheet 未命中时的兜底。
3. **单位清单包含匹配**（取最长命中）——两路规则均未命中时兜底。

实现 `org.UnitNormalizer.load_sheet(rows)` + `unit_of(values)`，sheet 目录只负责「班组/工区名 + 上下文 → 单位（工区）」，「单位（工区）→ 归列单位」仍由 `_match` 完成，二者解耦；sheet 缺失/未启用时优雅回退纯关键词路径，不阻断流水线。

### 2.7 管理承载力 F_管理（输入解析.管理承载力.是否启用=true 时输出 management 块）

```
实需人天 = Σ 每个现场每日占用人天（需同进同出=2 / 一般到位=1），多日作业按日叠加
可供人天 = 单位全部在册可用人员 Σβ × 工作日天数   （v2.5 起；与班组/负责人承载力同构）
F_管理   = 实需人天 ÷ 可供人天（预警同 §2.5 四级分级）
```

- **同进同出判定**：作业风险等级命中「需同进同出风险等级」（默认 二级/三级）占 2 人天，其余（四级/五级等）占 1 人天。
- **在家管理人员数**：人员档案中 角色/职务 任一命中「管理岗关键字」（管理/主任/副主任/书记/经理/总经理/副总经理/专责…）者计入；按 单位(工区) 优先、县公司 兜底 归到报告 17 单位列；单位无档案按 0 计（**无默认管理人数回退**，实事求是不瞎设）。**v2.5 起该数仅作展示、不再进入分母**；分母改用 `staff_by_unit` 的单位在册 Σβ × 工作日天数。
- **输出结构**：`management = {enabled, managers:{单位:{在家管理人员数}}, overall:{单位:{实需人天,可供人天,在家管理人员数,管理承载力,预警}}, periods:[{period,start,end,units:{...}}], days:{日期:{单位:{实需人天,管理承载力,预警}}}}`。报告只在 日/周/月 三份报告「单位承载力分析」末尾生成「2.管理承载力分析」自然段，3 份通知函无此段。
- **加载联动**：**人员档案现为必要数据源，Excel 一律加载**（班组真实分母 + 工作负责人真名均来自此，与 强制统一β / 管理承载力开关无关）；`管理承载力.是否启用=true` 时额外输出「在家管理人员数」与管理承载段落。

---

## 3. 配置速查（`config/capacity_config.json`）

**公式节**：标准日工时 / 工时边界 / 默认β / 强制统一β / 负责人上限 / 分母保护 / 作业风险系数θ / 预警分级（阈值90/75/50，>90 满载可超100%，**无停工档**） / 轻载文案 / 巡视·操作作业类型 / 默认班组类型 / **输出姓名脱敏（默认 false，报告/函件一律真名）** / 非工作日（是否剔除周末 现值 **true**、法定节假日 现列 **2026 全年**放假清单；v2.3 起仅作用于周期累加分母、日维度按自然日，见 §2.3.1） / **管理承载力**（每人可到现场数2 / 同进同出占用2 / 一般到位占用1 / 需同进同出风险等级[二级,三级] / 管理岗关键字）/ **函件触发**（长期作业连续天数阈值5 / 函件作业清单行数上限300）。

**输入解析节**：主工作表（默认「秋检计划」）、表头行号、数据起始行、时间列格式（excel序列号/字符串）、作业计划列映射（Excel列名→逻辑字段）、人员档案（是否加载；列映射含可选列 县公司/单位（工区）/职务）、**管理承载力（是否启用）**、组织架构映射（是否启用/单位清单/**优先读取源表组织架构 + 组织架构工作表**/班组归属规则）。

> 原则：改系数只改配置，勿直接改 `constants.py`（会被配置覆盖）。

---

## 4. CLI（`calcCapacity.py`）

```
python calcCapacity.py data.json|workplan.xls [--xls PATH] [--api 'JSON']
   [--config PATH] [--period 日|周|月|年] [--demo] [--out PATH]
```
流程：用法校验 → 读配置应用公式 → 数据源路由 → `CapacityService.summarize` → 姓名脱敏 → 可选原子写盘 → stdout 出 JSON。

---

## 5. 报告生成链路

```
【月报 native】calcCapacity → genReportParams（→params）→ fillReport（正文草稿，保留模板 13 图位）
  → renderNativeCharts（Word/WPS 原生可编辑 Chart 直落 13 图位 → .docx 成品）
【日/周报 与 --chart png 回退】calcCapacity → genReportParams → renderReportCharts（PNG）
  → fillReport（正文草稿）→ applyCharts（插图 → .doc 成品）
一键：runReports.py --source 计划 --template 报告-日|周|月（--all） --report-date
      [--start --end] [--chart native|png] --out-dir
      （月报默认 native；--chart png 强制回退位图）

【函件】runReports.py --template 警示函-满载|提示函-重载|提示函-长期作业（同源同参数）
  一键：calcCapacity(--period 日) → genLetterParams（result+source→letters.json）
  → 逐封 fillReport（无图表）→ 按 basename 落盘成文 .doc
```

| 脚本 | 要点 |
|---|---|
| `genReportParams.py` | 读 calc 结果 + 计划源 → `{replace, paragraph_replace, tables}`：概况句(计划总数/二级/三级)、三级及以上风险明细表（**日/周含 工作负责人真名 + 班组成员人数 两列，月/年不输出人员**；日表 6 列、周表 7 列含 高风险作业时间、月表 5 列；高风险作业时间列=日期+单元格内软换行(\\v)+（N天），如「9月14日-15日」换行「（2天）」）、作业承载评述段(满载/重载分句)、真实管理承载段(F_管理=Σ实需人天÷Σ可供人天，日/周/月逐日聚合)、周报日承载力矩阵表(**转置：一行一个日期、每一列=一个单位分类**，首列「日期」、列头为各分类名，替代模板旧「单位×日期」布局；列名按 matrix 实际收敛，无计划日显示"-"，**列默认含全部出现日期即计划时间；开启工作日剔除后仅列工作日**；spec 附 `anchor:「各单位日计划如表所示」` 供 fillReport 定位目标表)；月报 2~5 周用 `paragraph_replace(anchor=「（1）作业承载力分析」, occurrence=周序号, next_paragraph)` 整段覆盖模板硬编码示例段。**AI 辅助建议注入**：`--advice advice.json`（`{"报告-日":…,"报告-周":…,"报告-月":…}`，缺条目回退默认句）→ replace 以模板占位段完整壳文本「（本期辅助建议由研判人员结合承载力数据综合提出。建议占位符：ADVICE_BODY_2026）」为键整段替换成纯建议正文，注入文末「四、辅助建议与风险管控提示」章，不再残留「建议占位符：」与占位符。参数 `--result/--source/--template/--report-date/--config/--out/--advice` |
| `genLetterParams.py` | 读 calc result(matrix) + 计划源，按模板触发规则筛选命中单位/负责人，逐封生成 fillReport 可消费参数（replace+tables）；无命中 `letters:[]`（ok JSON 退出码 0）。占位：文号 `（20××年第××号）`、统计区间长键、单位短语（`××公司××工区/××中心（若…“××班组”）`→`国网青岛供电公司{单位}`，清除模板编辑括注）、抬头 `××单位：`、成文/落款日期→周期末日。作业清单附表 10 列（序号/工作日期/工作内容/电压等级/风险等级/工作负责人/作业班组/自有人员数量/外包施工单位/外包人数），外包两列「—」，**工作负责人输出真名（不脱敏）**，文件名非法字符转全角。参数 `--result/--source/--template/--report-date/--config/--out` |
| `fillReport.py` | 6 模板：警示函-满载/提示函-重载/提示函-长期作业/报告-日/报告-周/报告-月；`--params '{"replace":{占位:值},"paragraph_replace":[{anchor,occurrence,new_text[,next_paragraph]}],"tables":[{columns,rows[,anchor]}]}'`；占位符按长度降序替换；paragraph_replace 按 anchor 第 N 次命中替换，next_paragraph 模式替换命中段后的下一段(整段覆盖)；模板只读不写回。**表格定位**：优先按 `spec.anchor`（锚点句）定位“锚点之后第一个表”，兼容锚点句被吸进表首格的情形（命中区间落某表内→该表即目标，v1.8）；无 anchor 走表头重叠+列数一致的 `_match_score`。**转置矩阵表**到达目标表后按 spec 列数 动态删/补列（`Columns.Delete/Add`），列头逐格覆盖写。**防吞段**：正文段落改写只重写「段内文本、保留段落标记 \r」，避免整段 `Range.Text` 重写把紧邻其后表格上方的正文段（如「各单位日计划如表所示…」锚点句）吸进表首格、anchor 定位失灵；表头写入首格时同样保留被并入的前缀正文文本。**明细表 rows 为空**（该周期无三级及以上风险）时整表删除（含表头）：先读出概况句→删表→**查「无需列示明细。」是否仍在，仍在则不重建（防重复段）、否则**按「各作业类型日计划数量如下图所示」锚点重建（详见 v1.7/v1.8 变更记录），避免空表占用版面 |
| `renderReportCharts.py` | `{prefix}_单位承载力.png`（17单位横条，**预警分级配色：≤75% 深蓝、75~90% 浅蓝、>90% 红、>100% 深红（无橙黄）+75%/100%线；单位名每行 ≤6 字折行、单位条多时图表动态加高（min 2.3in、上限 2.85in）**）+ `{prefix}_作业类型.png`（**纵向柱状**：x 轴=作业类型、y 轴=计划项数，类型名折行/旋转防重叠）+（`--mgmt`）`{prefix}_管理承载力.png`（**各单位柱状图**：读 calc 的 `management.days` 对 lo~hi 按 Σ自然日实需÷(在册×工作日天数) 作柱高，按预警分级配色 + 柱顶标注数值，75%/90% 预警参考线）；`.xls/.xlsm` 走 xlrd、`.xlsx` 走 openpyxl，口径与 calcCapacity 一致；参数 `--data/--xls/--config/--lo/--hi/--label/--prefix/--out-dir/--no-ptype/--mgmt` |
| `renderNativeCharts.py` | **月报 Word/WPS 原生可编辑 Chart**（v2.12，`--chart native` 默认路径）。`--doc 草稿 --result calc.json --source 计划 --config --lo --hi --specs 周区间JSON --out xxx.docx`；`locate_slots(doc)` 读模板正文段落，按「图 N」图题识别图位（图1 概况类型、图2 全月作业、图3 全月管理、其后每周作业/管理；每槽=段序+图号+图题）；逐槽取数据——作业类型图用 `plan_type_count`（全局月份）、管理承力图用 `manage_period_mean`（按图题 `build_specs` 各自区间）、承载力图用 `unit_period_mean`——后 `add_native_chart`：**先 Select 图位段起点再无参 AddChart**（WPS 忽略 Range 位置参数必插文档开头）→ ChartType=横向条(xlBarClustered，类型图类少仍竖柱) → ChartData.Workbook 逐格写数据 → SetSourceData → Close(False)；配色 C_MAIN/C_HEAVY/C_FULL/C_OVER（≤75 深蓝/75~90 浅蓝/>90 红/>100 深红，RGB 字节序 r|g<<8|b<<16）；**成品须 .docx**（OOXML 保留可编辑性，.doc 会栅格化）；高度随条数变化、值轴 0~max(125,本月峰值)；末尾 `repair_layout` 只收敛图题孤行；ChartData 会先启 Excel 进程，运行后确保退出 |
| `applyCharts.py` | PNG 位图插入（default：日/周报 + 月报 `--chart png` 回退）。`--doc 草稿 --report day|week|month --charts 图目录 [--out]`；**月报**：按「图 N」图题段落定位图位（图1 类型、图2 全月作业、图3 全月管理、其后每周作业/管理），图位图座（idx-1）空槽直接 add_pic 插 PNG（`choose_cap_png` 按图题选 cap_m/cap_w1..w5_单位或管理应图），**不再做 insert_ptype/insert_manage_chart 附加插入（避免重复）**；**日/周**：原占位 InlineShape 替换 + 作业类型图插引导段后 + 管理承力图（`MANAGE_ANCHOR`）插评述段后新起段；插入用 **PIL 读 PNG 原始宽高等比缩放**（宽 435pt、超高 >420pt 时以高为准反算宽，`MAX_H_PT=420`，v2.17）；附件修版式 `fit_tables_to_text`/`repair_layout`；**入口统一绝对路径** + 有界重试 3 次兜底单实例 WPS 抖动 |
| `runReports.py` | 一键编排（v2.14 同进程 `main()`）。`--chart native`（默认，仅报告-月）→ `_pipeline` 完成 calc+params 后 `_native_pipeline` 只做 fillReport（正文草稿，保留 13 图位）→ renderNativeCharts(.docx 成品)；`--chart png` 或 日/周报 → `_pipeline`：calcCapacity → genReportParams → renderReportCharts（各周期 `--mgmt` 渲染管理图） → fillReport → applyCharts(.doc 成品)；`--all` 先 calc 一次再三份 skip_calc，整批共用隔离 Word/WPS；**函件**三步（calcCapacity → genLetterParams → 逐封 fillReport，无图表）；成品按模板命名规则落盘 `<out-dir>/承载力报告{YYYYMMDD}/`，中间产物 `_work/`（`--keep` 保留）；`--start/--end` 自定义范围透传（见 v2.8）；组织架构映射未启用时自动生成临时启用副本 `_config_org.json`；周/月区间复用 genReportParams 的 `_iso_week/_month_bounds/_month_weeks` 三口径一致；**并发约束**：同一时刻仅一路 WPS 自动化、每 runReports 独占 out-dir |

报告结构（周报/月报/秋检五章）与版式口径见《报告模板字段说明.md》与 SKILL.md 工作流；**日/周明细到 工作负责人真名 + 班组成员人数，月/年不输出人员（明细无负责人/人数列）；姓名一律真名（不脱敏）**。

### 5.1 函件触发规则（三份官方函件）

函件与报告共用同一 calc result（matrix）与计划源，单位口径一致；`runReports --template` 传入函件名即走函件三步编排，无图表。触发与占位要点：

| 函件模板 | 触发规则 | 命中粒度 | 数量 |
|---|---|---|---|
| 警示函-满载 | 周期内单位承载力**最大值 >90**（含 >100） | 单位 | 每命中单位一封 |
| 提示函-重载 | 周期内单位承载力最大值在 **(75, 90]** | 单位 | 每命中单位一封（与满载两档不重复发） |
| 提示函-长期作业 | 某工作负责人周期内**连续在岗作业天数 ≥ 阈值**（config「长期作业连续天数阈值」，默认 5） | 工作负责人 | 每人一封 |

- 周期 = 报告日所在 ISO 周（周一至周日）；文号 `2026年第{ISO周}号`。
- 满载/重载函单位短语按模板区分工区（满载）与中心（重载）两种编辑口径，统一替换为 `国网青岛供电公司{单位}`；长期作业函主题句替换为 `国网青岛供电公司{单位}工作负责人{真名}连续工作超{N}天（{lo}至{hi}）`。
- 作业清单附表 10 列行 = 命中单位/负责人周期内各计划；外包两列（外包施工单位/外包人数）填「—」（外包专项暂不开展）；**工作负责人输出真名（不脱敏）**；行数超 `函件作业清单行数上限`（默认 300）仅列示前 N 项并末行标注「其余略」。
- 无命中单位/负责人时 letters 为空数组（ok JSON，退出码 0），下游自行提示。

---

## 6. 错误码

| errorCode | 退出码 | 含义 |
|---|---|---|
| USAGE_ERROR | 2 | 参数错误 |
| INPUT_INVALID | 3 | 输入数据非法 |
| FILE_NOT_FOUND / FILE_INVALID / OUTPUT_WRITE_FAILED | 4 | 文件/IO 错误 |
| UNEXPECTED / DEP_MISSING | 1 | 未预期异常 / 缺 matplotlib |

stdout 只出结果 JSON（或 `{ok,false,errorCode,message}`）；日志写 stderr。

---

## 7. 维护纪律 + 自检

1. 模板只读，不得被结果覆盖
2. 敏感数据（身份证/电话/地址）不入计算与报告；**姓名一律真名（v1.5 不脱敏）**，配置「输出姓名脱敏」默认 false
3. 根副本 `安全作业承载力分析/` 与工作区嵌套副本 `安全作业承载力分析(工作区)/安全作业承载力分析/` 必须保持双份同步（若另有 `.sgcode/skills/` 技能副本亦同步）
4. 改系数改配置，不改常量
5. 工作日日历（代码为准）：config「非工作日.是否剔除周末」现值 true、「法定节假日」现列 2026 全年，`is_workday` **仅作用于周期累加分母**、分子按自然日计划累加、日维度按自然日展开（v2.15/v2.3，见 §2.3.1）；需完全按计划时间算时把开关置 false、清空清单
6. 改完跑自检 + 在本表记一笔

```bash
cd 安全作业承载力分析/scripts
python calcCapacity.py --demo        # 期望退出码 0
python fillReport.py --list
diff -r --exclude=__pycache__ 安全作业承载力分析 .sgcode/skills/安全作业承载力分析
```

### 变更记录

| 版本 | 日期 | 修改摘要 |
|---|---|---|
| v2.27 | 2026-09-17 | **AI 建议+所属机构：单份/两份/--all 同一硬闸**：`validate_advice_for_templates` 按本批模板校验 `--advice`；`runReports` 出报告必 `--advice`、报告与函件均必 `--office`；新增 `--templates` 同批两份。SKILL 第 0 步/命令表同步。双份同步 |
| v2.26 | 2026-09-17 | **报告侧文件合并（方案 B）**：`report_layout`+`renderNativeCharts` 并入 `capacity/office_chart.py`；`renderReportCharts`+`applyCharts` 合并为 `pngCharts.py`（子命令 render/apply）；保留薄入口 `nativeCharts.py`。脚本由约 21 个减至约 19 个。E2E：`--all` native 通过；PNG apply 遇 RPC 死链时 `recycle_shared_word`+独立实例重试，插图前回收共用 Word。SKILL/手册同步。双份同步 |
| v2.25 | 2026-09-17 | **确定性+--all 稳跑**：SKILL 固化「同一输入→同一计算/版式；仅 advice/--office 因人而异；完善技能默认不交付生成报告」。`fillReport.recycle_shared_word` + Open 失败重试；`--all` 日→周→月之间回收共用 Word；`polish_month_docx` 优先复用 shared、禁止双实例互抢。双份同步 |
| v2.24 | 2026-09-17 | **版面策略固化为可复用规则**：新增 `capacity/report_layout.py`（表居中/列宽/明细行距、图宽、全月/周/类型图高区间、`polish_month_docx` 按版心剩余空间动态定稿）。`fillReport._normalize_report_tables` / `renderNativeCharts._vbar_height` / `office_chart.reflow_month_type_chart` 只读该模块；删除一次性 `_com_polish_month_layout`。SKILL 固化「固定流水线：除 AI 建议与函件 `--office` 外全部代码自动；改观感只改 report_layout」。双份同步 |
| v2.22 | 2026-09-17 | **排版弹性收紧**：自查实页后改分页——图1独页、图2+图3同页、（二）+第1周同页、第2～5周各一页；去掉章题空白页与周内容互相穿插。图高约118~148pt。19页→13页。双份同步 |
| v2.21 | 2026-09-17 | **国网竖柱+月报固定分页**：日/周/月统一竖柱（横轴=类别、纵轴=%/项）；无外框无图例；分类轴禁倾斜（类多换行）。月报计划表后换页进图区，每周标题段前分页（一周一页）。PNG 回退同步竖柱。双份同步 |
| v2.20 | 2026-09-17 | **排版粘死+改回横向条**：标题/引导与图 KeepWithNext 整组，空段压扁；图高封顶 240pt 防空半页。统一横向条（类别横排不旋转）。标签整数无单位（轴标题才写 %/项）。双份同步 |
| v2.18 | 2026-09-17 | **月报可编辑图改 OOXML 写入**：不再 PNG、不再 WPS `AddChart`/`ChartData`（内网 et 易耗尽）。`capacity/office_chart.py` 用 openpyxl 内嵌数据簿 + DrawingML `c:barChart` 写入 `.docx`；fillReport `.docx` 后缀不再误判成 `.doc`。双份同步 |
| v2.17 | 2026-09-17 | **月报方案 C**：图号改阅读序（图1 概况类型、图2 全月作业、图3 全月管理、其后每周作业/管理）；作业/管理原生图改横向条 `xlBarClustered`、高度随条数、本月共用 0~max(125,峰值) 刻度；类型图类≤8 竖柱。模板图题加空格、图+图题 KeepWithNext，不钉死段前分页。不足 5 周删多余周节。PNG 回退同视觉，`MAX_H_PT=420`。双份同步 |
| v2.15 | 2026-09-17 | **周期累加分子保留周末/节假日计划**（用户："计算多天承载力，计划中有周六周日的，分母去掉周六周日和节假日，分子不去掉"）：①`calc_period` 不再对非工作日 `continue` 整段；分母仍仅工作日 Σβ×8h，分子对区间全部自然日 `_on_day_plans` 累加工时；②`_mgmt_range_summary` 实需按全部自然日加总、天数=`len(_workdays_between)`；③`genReportParams._mgmt_aggregate` 同步（day_list=自然日、span_days=工作日）。日维度/矩阵/图表日均值未改。双份同步工作区嵌套副本 |
| v2.14 | 2026-09-17 | **编排性能（不合并模块）**：①`runReports._run` 改为同进程 `main(argv)`，去掉每步 `subprocess.run`；②月报 native 不再在 `_native_pipeline` 重跑 calc/params；③`--all` 先 calc 一次，日/周/月 `skip_calc`；④`fillReport` 提供 `begin_shared_word`/`end_shared_word`/`current_word_app`，applyCharts/renderNativeCharts 复用同一隔离 Word/WPS，直接 CLI 仍各自 DispatchEx；⑤`repair_layout` 维持上限 8 轮。文档 SKILL 固化命令块同步。模板 COM 巡检：无「。。」/Excel 注记则不改 OLE。双份同步含工作区嵌套副本 |
| v2.13 | 2026-09-16 | **全量死代码精简（删除周合并聚类/--strip/冗长副产）+ references 瘦身**（用户"脚本代码太多冗余删除、references 太多重复精简但全面"）：①renderNativeCharts 删除整段 `add_week_cluster_chart` 周合并图函数约 123 行、删除 `strip_native_slots` 及 parser `--strip`（13 图位模板下 strip 会误删第3周图位；PNG 回退由 applyCharts 按 13 图位图题填充，不再需清理）；②renderReportCharts 删除 `_WEEK_BLUE`/`render_week_cluster_chart`/`--week-cluster`/`--weeks`，--lo/--hi 必填；③runReports 删除 _pipeline 周合并渲染块与 --strip 调用；④applyCharts 月报重写 `replace_capacity_shapes` 按图题定位全部 13 图位填充（空槽直接插图）+ 月报跳过 insert_ptype/insert_manage_chart（防重复）、删 `locate_para_index` 死代码；⑤genReportParams/genLetterParams 删 SCRIPT_DIR/KEY_FIG_CAPTIONS/parse_date_opt/SimpleNamespace 等死代码；⑥references 删 实现方案.md/报告模板结构说明.md（过时），更新 承载力量化分析方法.md（真实档案+预警口径）、环境部署（补 pywin32/matplotlib/Pillow 依赖）；全部脚本 py_compile+AST 验证通过，双目录 diff -qr 零差异 |
| v2.12 | 2026-09-16 | **月报 13 图位原生可编辑 Chart（弃周合并聚类）**（用户否决 v2.11 周合并方案"不要周合并图"，模板改预排 13 图位：图1 全月作业 + 图2 全月管理 + 图3~12 第1~5周作业/管理各1（作业=2K+1、管理=2K+2）+ 图13 作业类型；每周作业与管理必有图）：renderNativeCharts `locate_slots` 读正文分段识别图位=段序+图号+图题，每槽独立取数据（作业类型→`plan_type_count` 全月、管理→`manage_period_mean` 按 `build_specs` 各自区间[图2 全月/图3~12 周]、承载力→`unit_period_mean`）；`add_native_chart`=Select 图位段起 → 无参 AddChart → ChartType xlColumnClustered → ChartData.Workbook 逐格写 → SetSourceData → Close；配色 C_MAIN/C_HEAVY/C_FULL/C_OVER；成品 .docx(OOXML)；修复**管理周图用全月数据的严重错误**（管理分支改按 build_specs 各自区间）；渲染后 repair_layout 标题图不分页；月报 --chart png 回退由 applyCharts 按图题填 13 图位 |
| v2.11 | 2026-09-16 | **月报改 Word/WPS 原生可编辑 Chart（4 图位）**（用户"月报用原生可编辑图表"）：renderNativeCharts 引入——AddChart 落点坑=WPS 忽略 Range 位置参数、须先 Select 目标图位段起点再无参 AddChart；ChartData.Workbook 仅早期绑定 DispatchEx 可用；**成品必存 .docx**（OOXML）否则栅格化；图位=图1 全月作业+图2 第1~5周合并作业+图7 管理+图8 作业类型（随后被 v2.12 13 图位取代） |
| v2.10 | 2026-09-16 | **输出目录规范**：成品一律收纳 `<out-dir>/承载力报告{YYYYMMDD}/` 日期子文件夹（不散放文件），中间产物 `_work/`，`--keep` 保留 |
| v2.9 | 2026-09-04 | **月报原生图表路径**：report-月 + native 走 renderNativeCharts（.docx），日/周与 png 回退走 applyCharts 五步（架构铺垫，详细 13 图位见 v2.12） |
| v2.8.1 | 2026-09-14 | **测试发现的锚定日-范围一致性校验**（用户："从测试的眼光去看看，有问题针对性修改"）边界测试 26 项：`_is_full_month`（9/11/12月整月、2月平/闰年、差一天、跨月）、`_pick_range_template`（1/2/7/8/28/29/30/31 天、跨月/跨年）全通过；fail 用例（非法日期 2026-02-30/非 ISO/start>end/缺 --end/日报多日/月报非整月/范围+--all）均 USAGE 退出码 2。**发现并修复：显式 `--report-date` 可落在范围之外**（如 9/1~9/10 + 锚定 10/15）——周报标题/文件名月份、月报周槽 label 按锚定日推导月份 →「10月」命名 vs 9 月数据脱节、日报图表按锚定日出图而正文按范围；修复=`runReports.py` main() 增加校验「锚定日必须落在 [start, end] 内，否则 USAGE 报错」（日报锚定=范围单日后图表/正文自然一致，命名逻辑无需改）；另确认：范围无数据（2027）在图表步 INPUT_INVALID「区间无单位承载力数据」明确报错不静默出空报告（可接受，未改）；26 项边界测试 + 全流程回归（9/1~9/10 周报）通过；SKILL.md 触发方式补「锚定日必须落在范围内」；双目录 diff 零差异 |
| v2.8 | 2026-09-14 | **支持用户自定义时间范围，范围外数据不进报告汇总**（用户："用户输入时间范围，生成报告，根据这个时间范围去判定，时间范围外的数据不写报告汇总"；拍板：类型按范围长度自动定、月报不到整自然月不生成、标题保留原周次/月次命名、函件跟随同一范围）①**`runReports.py` 新增 `--start/--end`**（YYYY-MM-DD 成对、start≤end）：`--report-date` 改可选（给范围时默认=范围结束日，可显式覆盖；锚定日只用于周次/月次命名、函件文号、成文日期）；新增 `_is_full_month`（1 日起、同月、末日止）与 `_pick_range_template` 自动判定——1 天→报告-日、2~7 天→报告-周、>7 天且覆盖整自然月→报告-月、>7 天未达整月→报告-周；显式 `--template` 优先，报告-月+非整月→报「月报需覆盖整自然月…不生成」，报告-日+多日→报错，范围+`--all`→报错；`_schedules` 周报主 spec/月报 spec 的 lo/hi 用范围（周次/周槽命名仍按锚定日推导）；报告/函件两条流水线向下游透传 `--lo/--hi`；输出 JSON 增 `"range": [start, end]`；②**`genReportParams.py` 新增可选 `--lo/--hi`**：`build_weekly_params` 的 `wk_lo/wk_hi` 取传入值（**ordinal 周次仍按 report_date 推导，保留原周次命名**），`build_monthly_params` 的 lo/hi 取传入值（整月由 runReports 保证，周槽仍 `_month_weeks`），`build_daily_params` lo=hi=该天；概况/明细/评述/日分析/矩阵表/管理承载全部自动跟随新区间（均消费 wk_lo/wk_hi，无需逐处改）；③**`genLetterParams.py` 新增可选 `--lo/--hi`**：`_period_meta(report_date, lo, hi)` 的 period lo/hi 取传入值、week/文号仍按锚定日；下游 `_unit_max_caps`/`_job_overlaps`/`_long_shift_teams`/`_common_replace`（区间文案「X月X日至X月X日」）/`_long_letter_replace`/basename 周次全部消费 `period["lo"]/["hi"]` 自动收敛；④`renderReportCharts.py` 的 `--lo/--hi` 本为必填入参，runReports 传对即收敛（不改）；⑤实测（工作计划-测试数据.xls，秋检 128 条 9 月）：9/1~9/10（10 天非整月）自动判报告-周、锚定 9/10 保留「第2周」命名，概况「9月份第2周（9月1日至9月10日）日计划5394项、三级11项」（原 9/7~9/13 口径 3679项/14项，9/11 及以后计划未出现）、Word COM 校验矩阵表 10 行（1日~10日）无占位残留；3 天 9/3~9/5→周报 1775项/矩阵 3 行/「第1周」命名；单日 9/5→自动日报 496项；整月 9/1~9/30→月报 15236项/三级 47项/管理承载 18%/5 周槽（回归）；函件 9/1~9/30+锚定 9/10→period 跟随范围、第37周/文号 2026年第37号按锚定日、区间文案「9月1日至9月30日」、全期无单位超 75% 故 0 封合理；三个报错用例（月报非整月/范围+--all/缺 --end）均输出 USAGE JSON（退出码 2）；双目录 diff 零差异 |
| v2.7 | 2026-09-08 | **分母/负责人全面改真实档案，删 20/10 瞎设默认（实事求是，匹配不到剔除）**（用户："按照β=1算；计划里公司/工区/班组都要匹配到，班组可多个也要匹配到，匹配不到的扔掉不算；工作负责人是**数量**，真名从班组/工区/公司匹配人员，瞎设数的删除；实事求是不得瞎写数据"）①**新增 `capacity/roster.py`**：班组名清洗（去 `-N` 抽调尾缀）+ 多班组拆分（顿号/逗号/斜杠/空白）+ 人员档案→班组索引（真实在册Σβ + 各班组工作负责人真名 + 工区/县公司计数）+ `resolve_team`（任一班组未命中返回 None）+ `filter_roster_matchable`（剔除班组匹配不到的计划）；②**`calculator.py` 分母改真实**：班组可用人数/负责人可用人天一律取 roster 真实在册（删 `DEFAULT_FULL_STAFF=20` 满编回退）；管理承载力 `_mgmt_unit_summary`/`build_management` 删 `DEFAULT_MANAGERS_PER_UNIT=10` 回退（无档案按 0）；`CapacityService` 接入 `build_roster_index`+`filter_roster_matchable`；③**工作负责人=数量**：`models.PlanRecord` 增 `leader_count`，`parsing` 把「工作负责人」列按整数数量解析（空/非数字按 1），负责人承载力分子按 `leader_count` 计、分母取该班组工作负责人真名数；④**`excelLoader` 人员档案一律加载**（真实分母必要源，表缺失优雅降级→相关计划剔除）；⑤**`genLetterParams.py` 长期作业按班组重键**（原按负责人列"1.0"数量分组已失效）：连续在岗落到「班组」粒度、封面句与作业清单工作负责人列输出该班组**真实工作负责人真名**（顿号拼接）、匹配不到的作业剔除（与 calc 同口径）；⑥`constants.py`/`config.py`/`config.json` 删除 20/10 回退项并更新说明；⑦文档同步（§2.1/§2.3/§2.4、`承载力量化分析方法.md` §3.5）；实测（工作计划-测试数据 - 副本.xls，秋检 128 条）：档案索引 176 班组/4385 人、128 条全匹配 0 剔除、多班组分母为真实求和（17/79/34/13）、管理按真实在册（变电检修 88人 10.5%、即墨 877 人）、长期作业函 4 封（变电检修 4 班组各一封，封面/附表负责人为真名）；双目录 diff 零差异 |
| v2.6.1 | 2026-09-07 | **可读性放宽**（用户反馈 v2.6 成品「太挤了，看不清文字」，定位=日承载力矩阵+图表+整体）①**矩阵字号 10.5→12pt**：fillReport `_compact_matrix_table` 签名改 `(tbl, ref=None, font_size=12.0)`，逐格**无条件** `df.Size=font_size`（含块 0 模板表；表级 Range.Font 读回 9999999 哨兵且表级赋值无效，只有逐格生效），字号/行高随 12pt 自动放宽，三块观感一致；②**矩阵列宽按版心重排**：新增 `_rebalance_matrix_widths`——首列（日期）60pt（12pt「8月31日」≈42pt+边距放得下），单位列宽=(版心宽−60)/最宽块单位数，17 单位 3 块 6/6/5 → 单位列 (442.2−60)/6≈63.7pt（12pt 4 字单位名 48pt+10.8 边距≈58.8pt<63.7，表头单行不折行），各块单位列等宽；`_fill_matrix_chunked` 每块归一后调用，`AllowAutoFit=False` 锁宽；实测 T2/T3=442.2pt、T4=378.5pt 全进版心；③**横向条形图加高**：renderReportCharts 新增 `HBAR_MAX_H_INCH=5.5`（原用 MAX_H_INCH 2.85），`_render_hbar_chart` fig_h 改 `min(5.5, max(2.30, 1.15+0.26n))`（原 `min(2.85, max(2.30, 1.10+0.155n))`），n=17 图高 2.85→5.5in；④**插图高度上限 200→300pt**：applyCharts `MAX_H_PT=300`（加高后 hbar 图按 PNG 原始比例反算宽 330~342pt，每条约 15pt 空间不拥挤；纵向柱状/管理图比例高 <300pt 不受影响）；实测周报 3 图均 330/324/342×300pt（原 ~410×200pt）；⑤**页数**（可读性优先于页数，用户确认）：日 3→4、周 4→5、月 13→14；checkV26Full 阈值周 ≤6/月 ≤15；块值与旧单表 0 不一致、17 单位列头齐全、无越界词；双目录 diff 零差异 |
| v2.6 | 2026-09-07 | **表/图「多数据自动换行（收窄/分块/转横向）」机制**（用户：风险工作明细表、日承载力矩阵表「横向太长看不全数据」，柱状图数量太多也要加此机制，保证数据清晰可见）①**宽表自动收窄**：applyCharts 新增 `fit_tables_to_text`（幂等，`apply_dynamic_layout` 后、SaveAs 前调用）——总宽超版心（A4 442.2pt）的表按表头语义下限（序号40/风险等级36/日期56/其余30pt，且不低于现宽）保下限、余量按比例分摊收窄并 `AllowAutoFit=False`；实测周报风险明细 7 列 763.8→442.3pt、月报风险明细 5 列 465.3→442.3pt，全列进版心数据完整可见；②**矩阵表单位列>8 自动纵切多块**：genReportParams `_matrix_table` 返回加 `max_units_per_table: 8`；fillReport `_fill_table` 单位列数>8 时走 `_fill_matrix_chunked`：平衡分块上下堆叠（17 单位→3 块 6/6/5，含日期列 7/7/6 列），块 0 复用模板既有表，块 1..n 用 `doc.Tables.Add` 接在前表后（表间以「（续表）」正文段分隔——WPS 保存会合并裸相邻表，新表首格写入会吞入段，须 `prev.Range.End` 处 `InsertAfter("（续表）\r")` 再在其后建表；边框 8 条/列宽从模板表快照+`AllowAutoFit=False` 锁定）；原填充逻辑抽 `_fill_table_data` 单表/分块共用（纯数据驱动，日/周/月任何模板自动适用）；实测周报矩阵 18 列 933pt→3 块 394/394/345pt，17 个单位列头全部进版心、各块值与旧单表逐格比对 0 不一致；③**新表单元格格式归一**（WPS 关键坑）：Tables.Add 新表单元格写值继承正文三号（16pt）字体+2 字符首行缩进——WPS 缩进按**字符单位**存储（`CharacterUnitFirstLineIndent=2`），按点赋 `FirstLineIndent=0` 不生效，须按字符单位清；16pt 字体在 49pt 列里逐字竖排（「16」拆「1」「6」两行、「市北中心」拆 3 行，行高撑到 ~63pt，周报 4 页→6 页）。`_compact_matrix_table` 统一：行自动高度+单倍行距+清缩进（字符/点双单位）+居中+**逐格**从模板表同位单元格复制字体（新表表级 `Range.Font` 读回返回 9999999 哨兵且表级赋值无效，只有逐格 Font 赋值生效；参照取模板表第 2 列，首列可能并入正文段格式混杂）；修复后周报回到 4 页、三块同观感 ~16pt/行；④**柱数>10 图表自动转横向条形**：renderReportCharts 新增 `MAX_VERT_BARS=10` 与 `_render_hbar_chart`（类别逐行铺 Y 轴、名称横排不旋转不折行、值标注条右端、>90 红粗、75/90% 参考线转 `axvline`、图高随条数自适应 1.10+0.155n、图张数与插入尺寸不变版式不膨胀），承载力量/作业类型/管理承载力三图分发；实测 17 单位图 1815×904px 插入 ~410×200pt 与旧图同尺寸；⑤**页数**：日 3=3、周 4（含分块矩阵+续表段）、月 11→13（明细表全列可见的行换行代价+建议章跨 2 页，可接受）；双目录 diff 零差异 |
| v2.5 | 2026-09-07 | **管理承载力口径改为「全部可用在册人员」为分母**（用户拍板："管理承载力的计算方法，是所有可用的人为分母，和班组成员承载力和负责人承载力一样"）①`calculator.py` 新增 `staff_by_unit()`（各单位全部在册人员 Σβ，人员侧 county+工区 归列与 `managers_by_unit` 一致）；`_mgmt_unit_summary` 分母由 `管理人数×每人可到现场数×天数` 改 **`单位在册Σβ×天数`**（取消 `×MANAGE_SITES_PER_PERSON`、不再限管理岗），输出新增「在册人数」字段；`build_management` 的 managers 输出与逐日 `days` 分母同步改新口径；`_mgmt_range_summary` 透传 staff_map；②`genReportParams.py` `_mgmt_aggregate`：`_mgr`（在家管理人员数）→ `_staff`（读「在册人数」，无档案回退 `DEFAULT_FULL_STAFF`），分母不再 `×MANAGE_SITES_PER_PERSON`；③`renderReportCharts.py` 管理承力图图注公式改「可供=单位全部在册人数×工作日天数」；④`references/承载力量化分析方法.md` §3.5 公式同步为 `F_管理=实需人天÷(单位全部在册可用人数×工作日天数)`；⑤实测（工作计划-测试数据.xls，W1=9/1~9/4 共4个工作日）：城阳中心 903.6%→**69.5%**、崂山 770.8%→45.8%、市北 459.4%→65.6%，全单位回落，9 月无单位触 75% 重载线 → 警示函/重载提示函按标准 0 封（函件仍按单位**作业承载**峰值 >90/75 判定，管理承载不参与触发）；⑥注：config「每人可到现场数」项保留不再参与公式；「在家管理人员数」仍输出（展示用）；双目录 diff 零差异 |
| v2.4 | 2026-09-03 | **模板/文本洁净度与评述句准确性优化**：①**模板管理句双句号修复**：日/周/月三份报告模板「2.管理承载力分析XXXXXXXXXX。。」等占位段自带双句号 → Word COM 段落级替换改单句号（日1段/周1段/月6段，`rng.Text = raw.replace('。。','。')` 保留段落标记；Find/Replace 对全角句号不生效，须段落级重写），改前备份 `temp/backup_templates_20260903_dd/`；②**删模板编辑注记**：三份模板「各作业类型日计划数量如下图所示：」后的「（按照Excel表中作业类型（D列）分类绘制柱状图）」独立注记段（日P35/周P39/月P32）Word COM 整段删除（`para.Range.Delete`），成品不再残留编辑注记（备份 `temp/backup_templates_20260903_note/`）；③**评述句按实际量值分级**：`genReportParams._build_job_block` 兜底句在全区间最大 cap<50% 时改「本期各单位作业承载力总体轻载、作业饱满度不高，未出现满载…」（旧一律「总体适中」，30%~40% 轻载期误报适中；profile cap 为**区间均值**口径，与承载力图柱高同源）；`_week_over_analysis` 日分析兜底句同步（区间峰值<50% →「各日承载力总体轻载」，**保留「各单位日计划如表所示」锚点前缀**，无数据仍兜底「总体适中」）；④**advice.json 周报均值/峰值更新**：旧 30.6%/33.7% 为 v2.3 前剔周末口径 → 新含周末全自然日 均值33.7%/峰值40.0%（周[9/1-9/6] 日值 30.1/32.1/26.4/33.7/40.0/40.0）；月报/日报建议数值经 128 项源核验一致未动（9月 技改+常规检修 92 项占 72%、三级 10 项、周 23 项占 79%、9/1 四级6/五级2、峰值 9/9 46.4/9/14 44.9/9/15 56.1）；⑤三份成品带 `--advice` 重跑，Word COM 校验：无「。。」/「按照Excel」/「（D列）」/占位/外包残留，评述句分级正确（日9-01、周[9/1-6]、月全月 mean=34.6% 及 6 周 mean 均<50 → 全部轻载），图 日3/周3/月8，周报矩阵表 8 行含周末、月报三级明细 10 项，双目录 diff 零差异，成品覆盖桌面 |
| v2.3 | 2026-09-03 | **日维度不剔除周末/节假日、周月累加分母保留工作日日历**（"计划中有周六周日和节假日不要去掉，周末承载力和日常一样；只有在计算月、周等累加时才考虑分母是不是节假日"）①**日维度放开周末节假日**：`calculator.py` `summarize` 的 day_set（days/matrix 展开）与 `management_demand_by_day`（management.days 逐日）由 `_workdays_between` 改 `_days_between` —— 计划覆盖的**全部自然日**展开，周六周日/法定节假日有计划的天照常参与日计算（测试数据 9/5 六、9/6 日、9/19 六、9/20 日 各 1 项：days/matrix/management.days 均已含，周末承载力 40% 与日常算法一致）；②**周期累加分母保留剔除**：`calc_period` 分子分母、`_mgmt_range_summary`/genReportParams `_mgmt_aggregate` 区间聚合分母仍按 `is_workday` 工作日日历剔除周末+法定节假日（不变，与"累加才考虑分母节假日"吻合：月报"全月管理承载力 38%"、变电检修可供人天 504 = 12 人×2×21 个工作日=30-8 周末-1 中秋）；③**周报日段矩阵表/周日分析句放开**：`genReportParams.py` `_matrix_table` days 枚举去掉 `is_workday` 过滤改逐自然日（周报日段表由 6 行→8 行，新增 9/5、9/6 且有值，8/31 无计划 '-'），`_week_over_analysis` 周日遍历去掉 is_workday 过滤；④`constants.py` `is_workday` 语义收敛为**仅周期累加分母**（docstring 注明日维度调用方不适用）、`config.py` 补 v2.3 注释；⑤**图自动含周末**：renderReportCharts `render_manage_chart`/`unit_period_mean` 按 lo~hi 自然日遍历 management.days/matrix，放开后月报管理承力图与作业承力图轴自动含周末点，无需改代码；⑥重新生成三份成品（日9-01/周9-03/月9-30），Word COM + calc JSON 双校验通过，四脚本已同步双目录 diff 零差异。注：`EXCLUDE_WEEKENDS=true` 与法定节假清单仍保留，但仅作用于周期累加分母 |
| v2.2 | 2026-09-03 | **报告模板删除外包章（"模板中的外包片段去掉"）**：①三份报告模板（日/周/月 .doc）用 Word COM 删除「三、外包队伍承载力分析」章节（标题段+正文段：日 P42/P43、周 P69/P70、月 P91/P92），「四、辅助建议与风险管控提示」顺延为「三」保持编号连续（模板段落数 日46→44、周72→70、月94→92，改前备份 `temp/backup_templates_20260903_outsource/`）；②genReportParams.py 删除 `KEY_OUTSOURCE_HOLDER` 常量与日/周/月三处 replace 条目（模板已无外包章，不再需「暂不列示。」占位替换；fillReport 对未命中 key 为安全 no-op，无影响）；③SKILL.md 辅助建议话题列表删「外包衔接」；④三份成品带 `--advice temp/advice.json` 重跑，Word COM 校验：章节编号 一/二/三 连续、全文无「外包」/「暂不列示。」/占位残留、辅助建议完整注入「三、辅助建议与风险管控提示」章、图表日3/周3/月8，已覆盖桌面旧件；⑤双目录 diff 零差异（函件作业清单附表「外包施工单位/外包人数」两列按模板固有表头保留、填「—」） |
| v2.1 | 2026-09-03 | **图表全改柱状图 + 辅助建议占位壳修复 + 外包先不做**：①renderReportCharts 作业类型图由横条改**纵向柱状图**（`ax.bar`，x 轴=作业类型、y 轴=计划项数，类型名折行/旋转防重叠）、管理承力图由逐日折线改**各单位柱状图**（读 `management.days` 对 lo~hi 每单位取**区间日均值作柱高**，按预警分级配色 + 柱顶标注数值 + 75%/90% 预警参考线，`render_manage_chart` 重写、`_MGMT_H_INCH=2.35in`），三图全柱状无折线；②**辅助建议占位壳修复**：genReportParams `KEY_ADVICE_BODY` 值改为模板占位段**完整壳文本**「（本期辅助建议由研判人员结合承载力数据综合提出。建议占位符：ADVICE_BODY_2026）」（三份模板占位段一致，一处改三份生效），fillReport 长键优先全文 replace 整段替换成**纯建议正文**——成品不再残留「建议占位符：」字样、占位不残留（原 v1.9 只替换内层 token 导致壳文字残留）；③**外包先不做**：外包专项暂不接入（外包两列/正文段维持 runReports 默认），报告外包队伍段保持「暂不列示。」（KEY_OUTSOURCE_HOLDER 仅日期带前缀，无需改动）；④三份成品（日9-01/周9-01锚9-03/月9-30）带 `--advice temp/advice.json` 重跑，Word COM 校验无「建议占位符」残留、图表数 日3/周3/月8 |
| v2.0 | 2026-09-03 | **管理承载力逐日趋势折线图**（外联共 3 类图，解决"管理承载力只有文字没图"①**renderReportCharts 新增 `--mgmt`**：读 calc 输出的 `management.days`（{日期:{单位:{实需人天,管理承载力,预警}}}）按 lo~hi 逐日取值，**一个有值单位一条折线**（`_MGMT_PALETTE` 深蓝同谱 6 色，少混色），逐日标注数值（>90 红粗体）、75%/90% 预警虚线 + 100% 上界参考（`axhline`），X 轴 >8 天旋转 45° 并降采样防标签重叠（`_short_date` 缩 M/D），图高 `_MGMT_H_INCH=2.35in` 插入宽 435pt 时高约 184pt 稳居模板图框 160~188pt 内；产出 `{prefix}_管理承载力.png`；②**applyCharts 新增 `insert_manage_chart`**：定位「本日/本周/全月管理承载力为 X%」评述段（`MANAGE_ANCHOR` 映射 day/week/month）后新起一段插入 `{prefix}_管理承载力.png`（图不存在/段缺失安全跳过）；main 在 `insert_ptype` 后调用；③**runReports `_pipeline` 图表循环主周期 spec（i==1，prefix=cap_d1/cap_w1/cap_m）附带 `--mgmt`**，周 spec 不重复渲染避免月报多张 spec 互相覆盖同名 `_管理承载力.png`；④三份报告成品图数：日/周 3 图（承载力争+作业类型+管理折线）、月 8 图（6 承载力争+作业类型+管理折线），管理折线 Word 内比例≈2.397 与原 PNG 一致、不拉伸；⑤config `输入解析.组织架构映射.是否启用` 持久化为 **true**（承载力争依赖 calc 的 matrix，全量数据按 17 单位组织归列；`_ensure_org_cfg` 临时副本逻辑仍保留为兜底）⑥**外包队伍段如实替换「暂不列示」**：genReportParams 新增 `KEY_OUTSOURCE_HOLDER`（"XXXXX。XX较重XXXXXX。"，日/周/月外包体段公有后缀）进三份 replace 统一替换为「暂不列示。」——外包数据未接入，不再残留模板 X 占位（成品「外包队伍承载力暂不列示。」） |
| v1.9 | 2026-09-03 | ①**模板删假引导句+文末辅助建议章**：日/周/月三份报告 .doc 删除「。各单位作业承载力如图所示」「，承载力较重的前10家单位承载力如图所示」等**无图框假引导句**（根治"引导句在、图没有"），仅保留真图框引导句（日「1.日作业承载力如图所示。」/周「1.本周…」，月 6 图框）；文末新增「四、辅助建议与风险管控提示」标题段 + 正文占位段（唯一占位符 `ADVICE_BODY_2026`，样式沿用模板正文段）；②**AI 辅助建议注入**：genReportParams 新增 `--advice advice.json`（`{"报告-日":…,"报告-周":…,"报告-月":…}`，`_resolve_advice` 缺条目回退内置默认句）→ replace 加 `ADVICE_BODY_2026` 键全文替换占位不残留；runReports 透传 `--advice`；建议由 **AI(=当前生成者)结合当次 calc 数据实时撰写、不接外部 API**，两阶段确认流程见 SKILL.md 工作流第 5 步；③**applyCharts 图拉伸修复**：`add_pic` 以 PIL 读 PNG 原始宽高、**按原比例等比缩放**（宽 435pt、高随比例，超高 200pt 时以高为准反算宽，`LockAspectRatio=0` 后显式写宽高），不再先定宽再压高破坏比例（原比≈2.008，旧逻辑被拉伸/超高撑版式）；④**renderReportCharts 可读性**：条高 0.6→0.62、数值/轴标签字号 +0.5~1、标题 11.5→12、脚注 7.5→8；⑤文档同步：SKILL.md/报告模板字段说明/本手册三份文案随改（双副本同步见 §7） |
| v1.8 | 2026-09-03 | ①**周报矩阵表转置**：改「一行一个日期、每一列=一个单位分类」（首列「日期」、列头为分类名，替代模板旧「单位×日期」布局），genReportParams `_matrix_table` 输出 anchor「各单位日计划如表所示」；②**fillReport 矩阵填充修复**：anchor 定位兼容锚点句被吸入表首格（命中区间落某表内→该表即目标，修复误选首表/只补 4 列/空表残留，根因=正文段整段 `Range.Text` 重写把紧邻表格上方的锚点句吸进表首格、anchor Find 命中区间落表内导致定位失效→score 兜底误选）；正文改写改「段内文本重写保留段落标记 \r」根治吞段；表头写入首格保留并入前缀文本；按 spec 列数动态删/补列；空表删除后查「无需列示明细。」仍在则不重建概况句（防重复段）；③**图表排版**：承载力图配色去橙黄（75~90% 改 `#5A8FD4` 浅蓝深蓝同谱，全图 深蓝→浅蓝→红→深红）、单位名每行 ≤6 字折行、条多时图表动态加高（min 2.3in / 上限 2.85in，`_wrap_cn`/`MAX_H_INCH`）、applyCharts 插入锁纵横比高超 200pt 时以高为准反算宽（`MAX_H_PT`），图不挤不撑版；④**模板去黄**：6 份官方 .doc 模板章节标题/表头自带黄色高亮（`HighlightColorIndex=7` wdYellow）全部清除（副本处理+原子替换，双副本 templates 同步） |
| v1.7 | 2026-09-03 | 明细表空块移除：周期内无三级及以上风险作业（genReportParams 明细 rows 为空）时，fillReport `_fill_table` 整表删除（含表头），概况句由 genReportParams 改写为「…当日/本周/当月无三级及以上风险作业，无需列示明细。」收尾，不再残留空表与悬空“…明细表如下：”占版面。实现：先 Find「无需列示明细。」读出概况句 → 删表 → 按「各作业类型日计划数量如下图所示」锚点段首 InsertBefore 重建概况句（规避 `Range.Text` 改写段与表格粘连、删表吞句/吸段问题）；非空明细（如周/月报有多条三级计划）不受影响、按原样填表 |
| v1.5.1 | 2026-09-02 | 去掉对周六周日与法定节假日的判断：工作日日历**默认不剔除**、完全按计划时间计算（config「非工作日.是否剔除周末」默认 false、「法定节假日」默认空清单，`constants.EXCLUDE_WEEKENDS=False`）；`C.is_workday` 保留为可选开关，需剔除时在 config 开启/维护清单即可。生效点（days/matrix、calc_period、管理承载力、周报矩阵列/日分析枚举）默认含全部计划日期 |
| v1.5 | 2026-09-02 | 承载力口径调整：①名字不脱敏（输出姓名脱敏=false，报告/函件一律真名，长期作业文件名去 `＊`）；②日/周明细表 工作负责人真名 + 班组成员人数 两列（日 6 列、周 7 列，模板加列），月/年不输出人员（月明细仍 5 列）；③每人每日“实际工时”≤8h 截断（负责人按人分组 8/ΣT、班组成员 ΣT×人数>8×人数 压缩），周期聚合不做人均截断；④可超 100%（删「停工(超满载)」档，>100% 落满载红）；⑤新增工作日日历：剔除周六周日与法定节假日（config「非工作日」节、`C.is_workday`），生效于 days/matrix、calc_period、管理承载力、周报矩阵列/日分析枚举；`output.py` 内置样例改真名 |
| v1.4.1 | 2026-09-02 | fillReport 出口校验输出文件名 Windows 非法字符（`\ / : * ? " < > |`）：命中即明确报错(退出码 4)，不上 Word 空跑。根因：文件名含 ASCII `*`（姓名脱敏「姓+*」未全角化）会让 WPS SaveAs 稳定报 3011「保存失败」，且与并发无关、重试救不回；非法字符全角化由 `genLetterParams._safe_filename` 负责，此处仅提前暴露问题点便于排查 |
| v1.4 | 2026-09-02 | 函件成套 + 组织架构读源表 sheet：新增 `genLetterParams.py`（calc result→逐封函件填充参数：满载>90/重载(75,90]/长期作业连续天数≥阈值三类触发，作业清单 10 列附表、外包「—」、负责人脱敏、文件名全角化）与 runReports 函件三步编排（calc→genLetterParams→逐封 fillReport）；组织架构归列支持读源 xls「组织架构」sheet（班组/工区名+公司/工区上下文消歧，县公司简称∈单位清单直归、直属按行内单位（工区）名走关键词，sheet 未命中回退关键词）；applyCharts 入口统一绝对路径（WPS COM 相对路径 Open 报 3010）+ 有界重试；config 增 长期作业连续天数阈值/函件作业清单行数上限/优先读取源表组织架构/组织架构工作表（§2.6.1、§5.1） |
| v1.3 | 2026-09-01 | 报告生成流水线完善：新增 `genReportParams.py`(result→填充参数：概况/三级以上明细/作业评述/真实管理承载段/周报日矩阵表)与 `runReports.py`(五步一键编排)；fillReport 支持 `paragraph_replace` 段落级替换(anchor+occurrence、next_paragraph 整段覆盖)；renderReportCharts 支持 `.xlsx`(openpyxl 双引擎)；applyCharts 支持日报告(`--report day` + 无图框兜底锚点)；周报三处内容修复(管理段前缀去重、日分析日期间无叠「日」、矩阵表按 matrix 实际单位收敛+无计划日"-") |
| v1.2 | 2026-09-01 | 新增管理承载力（§2.7 F_管理）：同进同出2/一般到位1人天、在家管理人员数统计、日/周/月报告段落；人员档案列映射补 县公司/单位（工区）/职务；management 块输出 |
| v1.1 | 2026-09-01 | 精简重排：压缩为规则速查 + 配置 + 链路，删除冗余表格 |
